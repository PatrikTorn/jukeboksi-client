// import { ENDPOINT, createActionPointers} from '../tools/actionTools';
//
// export const actions = createActionPointers([
// 	`LISTEN_PLAYLIST`,
// ]);
//
// export const listenPlayList = () => (dispatch, getState) => {
//
// }
//
//
// this.props.socket.on('get playlist', (playlist) => {
// const arr1 = playlist.map(pl => pl.title);
// const arr2 = this.props.app.playlist.map(pl => pl.title);
// if(!arraysEqual(arr1, arr2))
//   this.props.setAppState({playlist});
// });
